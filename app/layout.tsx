// app/layout.tsx
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'EC DOKS - Premium Hotel Supplies in Nigeria',
  description: 'Nigeria\'s leading supplier of premium hotel supplies including chafing gas, steak sugar, liquid soap, wine, and beddings.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}